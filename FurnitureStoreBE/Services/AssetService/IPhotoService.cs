using CloudinaryDotNet.Actions;

namespace FurnitureStoreBE.Services.AssetService
{
    public interface IPhotoService
    {
        //Task<ImageUploadResult> UploadImage<T>(T query, IFormFile file, string folder);
        //Task<List<ImageUploadResult>> UploadImages<T>(IQueryable<T> query, Guid id, List<IFormFile> files, string folder);
        //Task<ImageUploadResult> UploadImage<T>(IQueryable<T> query, string id, IFormFile file, string folder);
        //Task<List<ImageUploadResult>> UploadImages<T>(IQueryable<T> query, string id, List<IFormFile> files, string folder);
    }
}
